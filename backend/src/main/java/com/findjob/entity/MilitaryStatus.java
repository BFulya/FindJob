package com.findjob.entity;

/**
 * Enum representing military service status
 * Used for filtering job seekers
 */
public enum MilitaryStatus {
    COMPLETED,
    EXEMPTED,
    POSTPONED,
    NOT_REQUIRED
}
